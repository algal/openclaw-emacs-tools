{
  "name": "emacs-tools",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin: Emacs tools via emacsclient",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "openclaw": "file:../openclaw"
  },
  "scripts": {
    "check": "echo 'No local build step configured.'"
  }
}
